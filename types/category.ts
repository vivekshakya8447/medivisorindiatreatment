export type Category = {
  _id: string
  coverImage?: string
  description?: string
  displayPosition?: number
  label: string
  language?: string
  postCount: number
  seoData?: any // Define more specifically if needed
  slug: string
  title: string
  translationId?: string
  url?: string
}
